import React from 'react';
import { shallow } from 'enzyme';
import expect from 'expect';
import Form from '../../app/components/todos/form_create';

describe('todos form component', () => {
	it('should render something', () => {
		let wrapper = 
	});
});